endpoint: "http://prebid.smart-hub.io/pbserver/?pp={{Host}}&seat={{.AccountID}}&token={{.SourceId}}"
maintainer:
  email: "support@smart-hub.io"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - audio
      - native
  site:
    mediaTypes:
      - banner
      - video
      - audio
      - native
userSync:
  # smarthub supports user syncing, but requires configuration by the host. contact this
  # bidder directly at the email address in this file to ask about enabling user sync.
  supports:
    - redirect
